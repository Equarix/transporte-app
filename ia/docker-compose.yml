version: "3"

services:
  ia:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=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
      - OPENAI_BASE_URL=https://integrate.api.nvidia.com/v1
      - OPENAI_MODEL=deepseek-ai/deepseek-v4-flash

      - CHATWOOT_API_TOKEN=D11JKfJpzXtLyikK4KV7ptEG
      - CHATWOOT_BASE_URL=https://chatwoot-chatwoot.up0fay.easypanel.host

      - MAIN_API_URL=https://mssql-api.up0fay.easypanel.host/api

      - BOT_DOCUMENT_TYPE=DNI
      - BOT_DOCUMENT_NUMBER=99999999
      - BOT_PASSWORD=botpassword123
      - BOT_FIRST_NAME=Bot
      - BOT_LAST_NAME=Entrafesa
      - BOT_EMAIL=bot@entrafesa.com
      - BOT_PHONE=999999999
      - BOT_DATE_OF_BIRTH=2000-01-01

      - INTERNAL_SECRET_KEY=internal-secret-key-change-in-production
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - default

networks:
  default:
    name: entrafesa-network
