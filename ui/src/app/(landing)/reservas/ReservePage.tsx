import { ResponseBooking } from "@/interface/response.interface";

interface ReservePageProps {
  response: ResponseBooking;
}

export default function ReservePage({ response }: ReservePageProps) {
  return <div></div>;
}
